name: Secrets Secrets Hurt Someone

on: [push]

jobs:
  shh:
    runs-on: ubuntu-latest
    steps:
      - name: Repo Level
        run: echo ${{ secrets.SHH_REPO }}
      - name: Environment Level
        run: echo ${{ secrets.SHH_ENV }}
